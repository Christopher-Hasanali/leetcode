package problems;

import java.util.ArrayList;
import java.util.Collections;

public class Problem938 {
    /*ArrayList<Integer> a=  new ArrayList<>();
    public int rangeSumBST(TreeNode root, int L, int R) {
        Collections.sort(a);
        boolean flag=false;int sum=0;
        for(int i=0;i<a.size();i++) {
            if((int)a.get(i) == L) {
                flag=true;
            }
            if((int)a.get(i)==R) {
                sum += (int) a.get(i);
                return sum;
            }
            if(flag) sum+=(int)a.get(i);
        }
        return sum;


    }
    public void returnArray(TreeNode root) {
        if(root==null) return;
        returnArray(root.left);
        a.add(root.val);
        returnArray(root.right);
    }*/
}
